__pycache__/
*.pyc
*.egg-info/
dist/
build/
.eggs/
*.egg
.pytest_cache/
.ruff_cache/

# IT Agents Guardrails
.env
.guardrails-report.json
.guardrails-human-log.jsonl
.guardrails.sarif

# Secrets — never commit these
*.pem
*.key
id_rsa
id_ed25519
credentials.json
secrets.json
terraform.tfvars
*.tfstate
*.tfstate.backup

# Container artifacts
*.tar
it-agents-guardrails-*.zip
IT-AGENTS-GUARDRAILS-MANIFEST.md

# Finance advisor local memory store (personal financial context).
# Both paths: the app moved to apps/ in the Turborepo restructure and the stale
# rule let the store become tracked. Never let this file into a commit.
projects/finance_advisor/data/memory.db
apps/finance_advisor/data/memory.db

# Node / Turborepo workspace
node_modules/
.turbo/

# Beta Queries crawl output (catalog snapshots — regenerate, never commit)
.bq-catalog/

# Beta Queries download bundle (regenerate: python scripts/beta_queries_bundle.py)
output/beta-queries.zip
